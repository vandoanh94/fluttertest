import 'package:flutter/material.dart';

class CardContainer {
  int id;
  Color color;
  int number;
 
  CardContainer(int id, Color color, int number) {
    this.id = id ;
    this.color = color;
    this.number = number;
  }
}